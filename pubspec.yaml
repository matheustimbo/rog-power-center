name: rog_power_center
description: ROG Power Center - Hardware control for ASUS ROG Strix laptops
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  tray_manager: ^0.5.2
  window_manager: ^0.4.3
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
